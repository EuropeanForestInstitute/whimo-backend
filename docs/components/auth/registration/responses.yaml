RegisteredResponse:
  description: User registered successfully
  content:
    application/json:
      schema:
        allOf:
          - $ref: '../../common/schemas.yaml#/DataResponse'
          - type: object
            properties:
              message:
                type: string
                default: Registered
